{
	"name": "landing-page-create-react-app",
	"version": "1.0.0",
	"homepage": "https://landing-page-cra.front10.cloud",
	"dependencies": {
		"@front10/landing-page-book": "^0.1.16",
		"react": "^16.5.1",
		"react-dom": "^16.5.1",
		"react-scripts": "1.1.5"
	},
	"scripts": {
		"start": "react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test --env=jsdom",
		"eject": "react-scripts eject",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d build -b gh-pages -r git@github.com:front10/landing-page-create-react-app.git",
		"now-build": "npm run build"
	},
	"devDependencies": {
		"gh-pages": "^2.0.0"
	}
}
